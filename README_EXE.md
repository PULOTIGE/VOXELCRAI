# 🚀 МЕГА EXE файл в репозитории

## 📦 Где найти EXE?

EXE файл находится в: **`dist/windows/adaptive-entity-engine.exe`**

## ⚠️ Важно

**EXE файл НЕ собран автоматически** из-за ограничений кросскомпиляции.

## 🔧 Как добавить EXE в репозиторий?

### Вариант 1: GitHub Actions (автоматически)

1. Запушьте код в репозиторий
2. GitHub Actions автоматически соберет EXE на Windows runner
3. EXE будет добавлен в репозиторий автоматически

**Workflow:** `.github/workflows/build-exe.yml`

### Вариант 2: Локальная сборка на Windows

```cmd
# Соберите EXE
scripts\build-mega-exe.bat

# Добавьте в git
git add dist/windows/adaptive-entity-engine.exe
git commit -m "Add built Mega EXE file"
git push
```

### Вариант 3: Использовать скрипт

```bash
# После сборки EXE
./scripts/add-exe-to-repo.sh
git commit -m "Add built Mega EXE file"
git push
```

## 📋 Текущий статус

- ✅ Структура подготовлена: `dist/windows/`
- ✅ README для пользователей создан
- ✅ GitHub Actions workflow настроен
- ⏳ EXE файл ожидает сборки

## 📝 Документация

- `ADD_EXE_TO_REPO.md` - Подробные инструкции
- `dist/windows/EXE_BUILD_STATUS.md` - Статус сборки
- `BUILD_INSTRUCTIONS.md` - Инструкции по сборке

## 🎯 Что дальше?

1. **Если у вас Windows:** Запустите `scripts\build-mega-exe.bat`
2. **Если используете GitHub:** Запушьте код, Actions соберет автоматически
3. **Если есть готовый EXE:** Скопируйте в `dist/windows/` и добавьте в git

---

**Примечание:** EXE файлы исключены из `.gitignore` только для `dist/**/*.exe`, поэтому они будут добавлены в репозиторий.
