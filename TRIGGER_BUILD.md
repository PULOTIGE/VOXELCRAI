# 🚀 Запуск автоматической сборки EXE

## Проблема с локальной сборкой

Локальная сборка Windows EXE на Linux требует:
- ❌ mingw-w64 toolchain (нет в системе)
- ❌ Права root для установки (нет доступа)
- ❌ MSVC linker для MSVC target (нет Windows SDK)

## ✅ Решение: GitHub Actions

GitHub Actions автоматически соберет EXE на Windows runner!

## Как запустить сборку СЕЙЧАС:

### Вариант 1: Через GitHub UI (самый простой)

1. Перейдите на GitHub в ваш репозиторий
2. Откройте вкладку **Actions**
3. Выберите workflow **"Build Mega EXE"**
4. Нажмите **"Run workflow"** → **"Run workflow"**
5. Дождитесь завершения (5-15 минут)
6. EXE будет автоматически добавлен в репозиторий!

### Вариант 2: Через push (автоматически)

Просто запушьте код:
```bash
git push
```

Workflow запустится автоматически и соберет EXE.

### Вариант 3: Через API (программно)

```bash
# Установите GitHub CLI если нужно
# gh auth login

# Запустите workflow
gh workflow run build-exe.yml
```

## Что произойдет:

1. ✅ GitHub Actions запустит Windows runner
2. ✅ Установит Rust и необходимые инструменты
3. ✅ Соберет МЕГА EXE файл
4. ✅ Добавит EXE в репозиторий автоматически
5. ✅ Создаст артефакт для скачивания

## Результат:

После успешной сборки:
- 📦 EXE файл будет в `dist/windows/adaptive-entity-engine.exe`
- 📥 Артефакт будет доступен для скачивания в Actions
- ✅ Файл будет закоммичен в репозиторий

## Время ожидания:

- ⏱️ Сборка: 5-15 минут
- 📊 Размер EXE: 80-150 МБ

---

**Рекомендация:** Используйте GitHub Actions - это самый надежный способ получить EXE файл!
