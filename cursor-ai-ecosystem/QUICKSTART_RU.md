# Быстрый старт - Cursor AI Ecosystem

## Установка

### 1. Клонирование или загрузка проекта

```bash
# Если у вас есть доступ к Git репозиторию
git clone <url>
cd cursor-ai-ecosystem

# Или просто распакуйте архив
```

### 2. Создание виртуального окружения (рекомендуется)

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

## Запуск

### Базовый запуск

```bash
python main.py
```

Это запустит экосистему с:
- 100,000 нуклеотидов
- 400 вокселей
- Визуализацией в реальном времени
- Поиском концептов

### Параметры запуска

```bash
# Больше нуклеотидов (медленнее, но полнее)
python main.py --nucleotides 1000000

# Больше вокселей
python main.py --voxels 1000

# Без визуализации (только консоль)
python main.py --no-visualization

# Без поиска концептов
python main.py --no-search

# Комбинация параметров
python main.py --nucleotides 500000 --voxels 800 --no-search
```

## Что вы увидите

### Визуализация

После запуска откроется окно с:

1. **Левая панель** - статистика в реальном времени:
   - Uptime (время работы)
   - FPS (частота обновления)
   - Voxels Alive (живые воксели)
   - Avg Kaif (средний "кайф")
   - Avg Energy (средняя энергия)
   - Concepts Found (найдено концептов)

2. **Правая панель** - сетка вокселей:
   - Зеленые ячейки = живые воксели
   - Яркость = уровень "кайфа"
   - Красные = мертвые воксели

3. **Нижние графики**:
   - Kaif History - история "кайфа"
   - Entropy History - история энтропии
   - Energy History - история энергии

4. **Концепты** - последние найденные термины

### Консольный вывод

Каждые 5 секунд выводится статистика:

```
[Stats] Uptime: 25.3s | FPS: 59.8
  Voxels: 398/400 alive
  Avg Kaif: 0.0234
  Avg Energy: 98.45
  Concepts: 15 unique
```

### Поиск концептов

Каждые 60 секунд (в тестовом режиме) программа:
1. Формирует поисковый запрос из ключевых слов
2. Ищет в DuckDuckGo
3. Извлекает новые термины
4. Добавляет их в базу знаний

Пример:
```
[Ecosystem] Поиск новых концептов...
[Ecosystem] Найдено 12 концептов: ['Neural', 'Algorithm', 'Crimea', 'Intelligence', 'Learning']
```

## Сборка в EXE

Для создания standalone исполняемого файла:

```bash
python scripts/build_exe.py
```

После успешной сборки файл будет в папке `dist/`:
- Windows: `dist/CursorAI.exe`
- Linux/Mac: `dist/CursorAI`

## Управление

- **ESC** или закрытие окна - выход из программы
- **Ctrl+C** в консоли - остановка

## Архитектура

### Нуклеотиды
- 256 байт на каждый
- Базовые ячейки памяти (A, T, G, C)
- Эпигенетические метки
- Семантические векторы
- Обновляются каждый кадр

### Воксели
- 9 КБ на каждый
- Микро-организмы с внутренней структурой:
  - Сенсоры (1.5 КБ)
  - Физика (1 КБ)
  - Мысли (2 КБ)
  - Эмоции (2 КБ)
  - Память (2 КБ)
- Система "кайфа" на основе энтропии эмоций

### Система "кайфа"

Кайф = |dS/dt| где S - энтропия эмоционального состояния

- Высокий кайф → усиление связей память-мысли
- Низкий кайф → ослабление связей
- Влияет на адаптацию и обучение

## Производительность

### Рекомендуемые настройки

**Слабый ПК:**
```bash
python main.py --nucleotides 10000 --voxels 100
```

**Средний ПК:**
```bash
python main.py --nucleotides 100000 --voxels 400
```

**Мощный ПК:**
```bash
python main.py --nucleotides 1000000 --voxels 1000
```

### Оптимизация

- Отключите визуализацию для ускорения: `--no-visualization`
- Отключите поиск концептов: `--no-search`
- Уменьшите количество нуклеотидов

## Проблемы и решения

### Низкий FPS

**Проблема:** FPS < 30

**Решения:**
1. Уменьшите количество нуклеотидов: `--nucleotides 50000`
2. Уменьшите воксели: `--voxels 200`
3. Отключите визуализацию: `--no-visualization`

### Ошибка импорта pygame

**Проблема:** `ModuleNotFoundError: No module named 'pygame'`

**Решение:**
```bash
pip install pygame
```

### Ошибка сетевого запроса

**Проблема:** Поиск концептов не работает

**Решение:**
- Проверьте интернет-соединение
- Отключите поиск: `--no-search`
- DuckDuckGo может блокировать частые запросы

### Медленный запуск

**Проблема:** Программа долго запускается

**Причина:** Инициализация миллионов нуклеотидов

**Решение:** Уменьшите количество: `--nucleotides 10000`

## Расширение

### Добавление своих ключевых слов

Отредактируйте `main.py`:

```python
self.concept_searcher = ConceptSearcher([
    '19V', 
    'CrimeaAI', 
    'ваши', 
    'слова', 
    'здесь'
])
```

### Изменение интервала поиска

В `main.py` измените:

```python
# 19 минут (1140 секунд)
self.ticker.add_task('search_concepts', self._search_concepts, interval=1140)
```

### Свои паттерны освещения

Создайте паттерны в `main.py`:

```python
from src.core import LightPattern

pattern = LightPattern()
# настройте паттерн
self.light_db.add_pattern(pattern)
```

## Дополнительная информация

- **README.md** - полная документация
- **src/core/** - основные компоненты
- **src/utils/** - утилиты (поиск, планировщик)
- **src/visualization/** - визуализация

## Лицензия

MIT & Apache 2.0

## Контакты

CrimeaAI Team - 19V Collective

---

**Приятного использования! 🚀**
