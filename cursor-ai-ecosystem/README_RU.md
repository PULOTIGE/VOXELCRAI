# Cursor AI Ecosystem 🧬

**Экосистема самоорганизующегося ИИ с нуклеотидами, вокселями и эмоциями**

[![Python Version](https://img.shields.io/badge/python-3.8+-blue.svg)](https://www.python.org/downloads/)
[![License](https://img.shields.io/badge/license-MIT%20%26%20Apache%202.0-green.svg)](LICENSE)
[![Status](https://img.shields.io/badge/status-active-success.svg)]()

> *"Сознание через хаос, порядок через энтропию"*  
> – CrimeaAI Team, 19V Collective

---

## 🚀 Быстрый старт

```bash
# Клонирование
git clone <repository-url>
cd cursor-ai-ecosystem

# Установка
pip install -r requirements.txt

# Запуск
python main.py
```

**Или используйте автоматические скрипты:**
- Windows: `run.bat`
- Linux/Mac: `./run.sh`

## 📖 Что это?

Cursor AI Ecosystem - это экспериментальная система искусственного интеллекта, построенная по биологическим принципам:

- 🧬 **62 млн нуклеотидов** - ячейки памяти с эпигенетикой
- 🎮 **Воксели-организмы** - микро-агенты с эмоциями и мыслями
- 💫 **Система "кайфа"** - обучение через производную энтропии
- 🔍 **Автопоиск** - интеграция концептов из интернета
- 🎨 **Паттерны освещения** - гибридный рендеринг
- 📊 **Визуализация** - состояние в реальном времени

## 🎯 Основные возможности

### Нуклеотиды (256 байт)
```python
nuc = Nucleotide(base='A')
nuc.update(dt=0.016)
expression = nuc.get_expression_level()  # 0.0 - 1.0
```

- **Базы**: A, T, G, C
- **Эпигенетика**: метилирование, ацетилирование
- **Семантика**: 39-мерный вектор смысла
- **Квантовый шум**: псевдослучайность

### Воксели (9 КБ)
```python
voxel = Voxel(voxel_id=1)
voxel.update(dt=0.016)
kaif = voxel.kaif  # "кайф" = |dS/dt|
```

**Структура:**
- 512 Б - метаданные (ID, позиция, возраст)
- 1.5 КБ - сенсоры (внешние стимулы)
- 1 КБ - физика (скорость, энергия)
- 2 КБ - мысли (обработка)
- 2 КБ - эмоции (состояние)
- 2 КБ - память (краткосрочная)

**Система "кайфа":**
```
kaif = |dS/dt|

где S = энтропия эмоционального состояния
```

- Высокий кайф → усиление обучения
- Низкий кайф → ослабление связей

### Паттерны освещения (1 КБ)
```python
pattern = LightPattern()
pattern.set_direct_lighting(directions, colors)
pattern.compute_sh_from_lighting()
color = pattern.evaluate_sh(normal)
```

- 32 источника прямого света
- 32 источника непрямого света
- 9 коэффициентов сферических гармоник
- Свойства материала (roughness, metalness)

### Поиск концептов
```python
searcher = ConceptSearcher(['19V', 'CrimeaAI', 'AI'])
concepts = searcher.search_concepts()  # → ['Neural', 'Learning', ...]
```

- Автоматический поиск каждые 19 минут
- Извлечение терминов из текста
- Интеграция в базу знаний

## 📊 Визуализация

После запуска откроется окно с:

```
┌────────────────────────────────────────────────────────┐
│  STATS              │         VOXEL GRID               │
│  • Uptime           │  ████████████████████████████    │
│  • FPS              │  ████████████████████████████    │
│  • Voxels Alive     │  ████████████████████████████    │
│  • Avg Kaif         │  ████████████████████████████    │
│  • Avg Energy       │  (20x20, цвет = кайф)           │
│  • Concepts         │                                  │
├────────────────────────────────────────────────────────┤
│  KAIF GRAPH         │  ENTROPY GRAPH                   │
│  ~~~^~~~^~~~        │  ^^^^^^^^^^^^                    │
├────────────────────────────────────────────────────────┤
│  ENERGY GRAPH       │  RECENT CONCEPTS                 │
│  ^^^^^^^^           │  • Neural                        │
│                     │  • Learning                      │
│                     │  • Algorithm                     │
└────────────────────────────────────────────────────────┘
```

## 🎮 Использование

### Базовый запуск
```bash
python main.py
```

### С параметрами
```bash
# Больше компонентов
python main.py --nucleotides 1000000 --voxels 1000

# Легкий режим
python main.py --nucleotides 10000 --voxels 100

# Без визуализации
python main.py --no-visualization

# Без поиска
python main.py --no-search
```

### Через Makefile
```bash
make install    # Установка
make run        # Запуск
make run-small  # Легкий режим
make examples   # Примеры
make build      # Сборка EXE
make clean      # Очистка
```

## 🔧 Сборка в EXE

```bash
python scripts/build_exe.py
# → dist/CursorAI.exe (Windows)
# → dist/CursorAI (Linux/Mac)
```

## 📁 Структура проекта

```
cursor-ai-ecosystem/
├── main.py              # Точка входа
├── requirements.txt     # Зависимости
├── config.example.py    # Пример конфигурации
│
├── src/
│   ├── core/           # Ядро
│   │   ├── nucleotide.py
│   │   ├── voxel.py
│   │   └── light_pattern.py
│   ├── utils/          # Утилиты
│   │   ├── concept_search.py
│   │   └── scheduler.py
│   └── visualization/  # Визуализация
│       └── display.py
│
├── scripts/
│   └── build_exe.py    # Сборка
│
├── examples/
│   └── simple_usage.py # Примеры
│
└── docs/
    ├── README.md
    ├── QUICKSTART_RU.md
    ├── ARCHITECTURE.md
    └── INSTALL_RU.md
```

## 🧪 Примеры

### Пример 1: Работа с нуклеотидом
```python
from src.core import Nucleotide

nuc = Nucleotide(base='A')
nuc.update(dt=0.016)
print(f"Экспрессия: {nuc.get_expression_level()}")
```

### Пример 2: Работа с вокселем
```python
from src.core import Voxel

voxel = Voxel(voxel_id=1)
voxel.update(dt=0.016)
emotions = voxel.get_emotional_state()
print(f"Кайф: {voxel.kaif}")
```

### Пример 3: Полная экосистема
```python
python examples/simple_usage.py
```

## 🎨 Архитектура

### Поток данных

```
Nucleotides (100k)
    ↓ expression
Voxels (400)
    ↓ kaif, emotions
Display (60 FPS)
    ↓ visualization
User
```

### Обновление вокселя

```
Sensors → Thoughts → Emotions → Memory
   ↑                              ↓
   └──────────────────────────────┘
```

### Система "кайфа"

1. Дискретизация эмоций в 20 бинов
2. Вычисление энтропии: `S = -Σ p(i) log p(i)`
3. Производная: `dS/dt = (S_current - S_prev) / dt`
4. Кайф: `kaif = |dS/dt|`
5. Адаптация связей на основе кайфа

## 📈 Производительность

| Параметр | Значение |
|----------|----------|
| Целевой FPS | 60 |
| Нуклеотиды | 100,000 (по умолчанию) |
| Воксели | 400 (по умолчанию) |
| Потребление RAM | ~256 МБ |
| CPU | 1-2 ядра |

### Рекомендации

**Слабый ПК:**
```bash
python main.py --nucleotides 10000 --voxels 100
```

**Средний ПК:**
```bash
python main.py  # по умолчанию
```

**Мощный ПК:**
```bash
python main.py --nucleotides 1000000 --voxels 1000
```

## 🐛 Решение проблем

### Низкий FPS
```bash
# Уменьшите компоненты
python main.py --nucleotides 50000 --voxels 200

# Или отключите визуализацию
python main.py --no-visualization
```

### Ошибка импорта pygame
```bash
pip install pygame
```

### Ошибка сетевого запроса
```bash
# Отключите поиск
python main.py --no-search
```

## 📚 Документация

- [README.md](README.md) - Полная документация
- [QUICKSTART_RU.md](QUICKSTART_RU.md) - Быстрый старт
- [ARCHITECTURE.md](ARCHITECTURE.md) - Архитектура
- [INSTALL_RU.md](INSTALL_RU.md) - Установка
- [CHANGELOG.md](CHANGELOG.md) - История изменений

## 🛠 Технологии

- **Python 3.8+** - язык программирования
- **NumPy** - векторные операции
- **SciPy** - научные вычисления
- **Pygame** - визуализация
- **asyncio** - асинхронность
- **BeautifulSoup** - парсинг HTML
- **PyInstaller** - сборка EXE

## 🔮 Будущие планы

- [ ] Multiprocessing для нуклеотидов
- [ ] GPU ускорение (CuPy/JAX)
- [ ] Сохранение/загрузка состояния
- [ ] Сетевой протокол
- [ ] Интеграция PyTorch
- [ ] Эволюционные алгоритмы
- [ ] WebGL визуализация
- [ ] Распределенные вычисления

## 📜 Лицензия

Dual-licensed: MIT & Apache 2.0

См. [LICENSE](LICENSE) для деталей.

## 👥 Авторы

**CrimeaAI Team - 19V Collective**

- Концепция: 19V
- Реализация: CrimeaAI
- GitHub: [TODO]

## 🙏 Благодарности

- Биологам за вдохновение эпигенетикой
- Нейробиологам за модель эмоций
- Сообществу Python за отличные библиотеки

## 📞 Контакты

- GitHub: [TODO]
- Telegram: [TODO]
- Email: [TODO]

## ⭐ Поддержка

Если проект вам понравился, поставьте звезду на GitHub! ⭐

---

**v1.0.0** | 2025-11-30 | CrimeaAI Team

*"Создавая искусственное сознание через хаос и порядок"*
