# ⚙️ Сборка EXE в процессе

## Текущий статус

EXE файл будет собран через **GitHub Actions** автоматически.

## Почему не локально?

Для сборки Windows EXE на Linux нужны специальные инструменты:
- mingw-w64 toolchain
- Windows SDK (для MSVC)
- Права администратора

Эти инструменты недоступны в текущей среде.

## ✅ Решение

GitHub Actions workflow (`.github/workflows/build-exe.yml`) автоматически:
1. Запустится на Windows runner
2. Соберет EXE со всеми зависимостями
3. Добавит EXE в репозиторий

## 🚀 Как запустить сборку:

### Способ 1: Через GitHub UI
1. Откройте репозиторий на GitHub
2. Вкладка **Actions** → **Build Mega EXE**
3. **Run workflow** → **Run workflow**

### Способ 2: Просто запушьте код
```bash
git push
```
Workflow запустится автоматически!

## ⏱️ Время ожидания: 5-15 минут

После сборки EXE появится здесь: `dist/windows/adaptive-entity-engine.exe`

---

**См. также:** `TRIGGER_BUILD.md` для подробных инструкций
